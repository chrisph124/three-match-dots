# Archived plan/spec records

Stateful pre-delivery records parked here after their work landed or was superseded.
Not authoritative — see `docs/` for current authority and `plans/` for active plans.

## superpowers/

The `superpowers` plugin's early default output location (`docs/superpowers/{plans,specs}`,
dated 2026-08-01 → 08-03). Superseded by the shipped Endless work and the current
`plans/` convention (`plans/journals/`, `plans/reports/`). Moved here 2026-08-18 to end the
docs/ drift; internal cross-links inside these files still use their original
`docs/superpowers/...` paths and are intentionally left as historical text.
